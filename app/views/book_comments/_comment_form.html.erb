<%= form_with(model:[book, comment], id: "new_comment") do |f| %> 
  <%= f.text_area :comment, rows: '5', class: "textbox", placeholder: "コメントをここに",size: '90 x5' %>
  <%= f.submit "Post Comment", class: "form_submit, btn btn-primary btn-base-1 mt-20 pull-right" %>
<% end %>