<%= form_with(model:[book, comment], id: "new_comment") do |f| %> <!--配列でインスタンス変数を2つ指定。コメントは、@bookに結びついた@comment-->
  <%= f.text_area :comment, rows: '5', class: "textbox", placeholder: "コメントをここに",size: '90 x5' %> <!--rowはtext_areaの高さ、初期値は2-->
  <%= f.submit "Post Comment", class: "form_submit, btn btn-primary btn-base-1 mt-20 pull-right" %>
<% end %>